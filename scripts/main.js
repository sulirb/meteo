import { main as current } from "../utils/current";
import { main as forecast } from "../utils/forecast";
import { main as forecastLT } from "../utils/forecastLT";

current();
forecast();
forecastLT();
